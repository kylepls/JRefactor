public <type.javaName> <name><if(genericDefine)>\<<genericDefine>><endif> 
    <if(extends)>extends <extends : {extend | <extend>}; separator=", "><endif>
    <if(implements)>implements <implements :{implement | <implement>}; separator=", "><endif>